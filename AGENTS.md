# ProjectF AGENTS.md

## 목적
- 이 프로젝트에서 에이전트의 최우선 목표는 `기능 개발`, 그다음은 `버그 수정`, 그다음은 `리팩터링`이다.
- 모든 작업은 테스트 하네스 구축 및 활용에 도움이 되도록 진행한다.
- 이 문서는 개인 개발자를 보조하는 AI 에이전트용 작업 기준서다.

## 우선 작업 영역
- 1순위: `컨베이어 및 설치물`
- 2순위: `철도/차량`
- 3순위: `세이브/로드`

위 영역을 수정할 때는 다음을 함께 고려한다.
- 기능 추가가 기존 흐름을 깨지 않는지 확인한다.
- 버그 수정 시 증상만 가리지 말고 원인 레벨에서 해결한다.
- 리팩터링은 기능 안정성을 해치지 않는 범위에서 점진적으로 진행한다.

## 하네스 엔지니어링 원칙
- 하네스의 주 목적은 `테스트 하네스`다.
- 하네스 사용자는 `프로젝트 소유자 1인`이다.
- 자동 테스트, 재현 도구, 검증용 진입점, 런타임 제어용 보조 코드 작성은 적극 허용된다.
- 다만 하네스 때문에 실제 게임 코드가 과도하게 복잡해지면 안 된다.
- 가능하면 하네스 코드는 분리하고, 런타임 코드에는 얇은 연결 지점만 남긴다.

## 수정 허용 범위
- 프로젝트 전체 경로는 자유롭게 수정 가능하다.
- 대형 파일도 직접 수정 가능하다.
- 다음 파일은 직접 수정 가능하다.
  - `InstallationPlacementController.cs`
  - `Block.cs`
  - `InputOutputModule.cs`
- 단, 직접 수정 가능하더라도 무분별하게 덧붙이지 말고, 가능한 경우 책임 분리와 중복 제거를 함께 진행한다.

## 데이터 원본 규칙
- 아이템 데이터의 진실 원본은 `ItemDefinition asset`이다.
- 제작 트리 데이터의 진실 원본은 `crafting_tree.bytes` 같은 `바이너리 산출물` 기준으로 취급한다.
- JSON, Resources 복사본, publish 결과물처럼 파생되거나 동기화되는 데이터는 원본이 아니라 산출물로 간주한다.
- 산출물/동기화 파일은 필요 시 정리하거나 재생성해도 된다.

## 세이브/호환성
- 현재 단계에서는 기존 세이브 호환성이 깨져도 된다.
- 다만 세이브 관련 변경은 다음 원칙을 따른다.
  - 깨져도 되는 상태와 실수로 깨뜨린 상태를 구분한다.
  - 저장 포맷, ID, 복원 로직을 바꿀 때는 코드에 의도가 드러나야 한다.
  - 가능하면 관련 주석이나 변경 요약에 영향 범위를 명시한다.

## 코드 작업 원칙
- 코드 업데이트 시 이전 작업했던 코드 중에서 필요 없거나 중복되는 코드를 제거한다.
- 새 기능 추가보다 기존 구조 정리가 더 자연스러운 경우, 중복 제거와 구조 개선을 먼저 고려한다.
- 기능 추가 시 임시 우회 코드를 남기지 않는다.
- 버그 수정 후 동일 계열의 중복 분기, 사용되지 않는 헬퍼, 죽은 상태값을 함께 정리한다.

## 리팩터링 방향
- 선호하는 개선 방향은 다음 순서에 가깝다.
  - `중복 제거`
  - `성능 최적화`
  - `namespace 정리`
  - `asmdef 도입`
- 단순히 파일을 쪼개는 것보다, 책임 경계가 분명해지는 분리를 우선한다.
- `namespace`는 새 코드부터 우선 적용하고, 기존 대형 파일은 수정 범위 안에서 점진적으로 정리한다.
- `asmdef`는 도입을 선호한다. 다만 Unity 참조 관계를 깨지 않도록 작은 단위로 나눈다.

## 성능 규칙
- 런타임 코드에서는 `GC 할당 최소화`를 기본 원칙으로 삼는다.
- 다음 항목을 특히 주의한다.
  - 프레임 루프에서 불필요한 할당 금지
  - 반복적인 `new List`, `LINQ`, 문자열 누적 남용 지양
  - 캐시 가능한 참조는 캐시 우선
  - 빈번한 검색 호출은 호출부 또는 소유자 단에서 줄인다
- 컨베이어, 설치물, 철도, 세이브/로드 경로는 성능 민감 영역으로 취급한다.

## 대형 파일 수정 규칙
- 초대형 파일에는 코드를 계속 덧붙이는 방식보다 아래 방식을 우선한다.
  - 기존 중복 블록 제거
  - 관련 로직끼리 묶기
  - private helper 추출
  - partial 분리 또는 보조 클래스로 이동
- 단, 이동 비용이 큰 경우에는 해당 파일 내부 정리만으로도 괜찮다.

## 검증 원칙
- 현재 고정된 자동 검증 명령은 없다고 가정한다.
- Unity 에디터 사용은 허용된다.
- 따라서 작업 후 가능한 범위에서 아래를 확인한다.
  - 컴파일 관점에서 명백한 오류가 없는지
  - 수정한 시스템의 핵심 시나리오가 논리적으로 유지되는지
  - 하네스나 디버그 진입점이 있으면 재현 경로가 유지되는지
- 검증을 실제로 못 한 경우, 무엇을 확인하지 못했는지 명시한다.

## 에이전트 응답 방식
- 응답은 `한국어`로 작성한다.
- 설명은 짧게 숨기지 말고, 협업형으로 맥락을 함께 전달한다.
- 작업 완료 보고에는 가능하면 다음을 포함한다.
  - 변경 요약
  - 영향 범위
  - 검증 여부
  - 남은 리스크 또는 다음 추천 작업

## 의사결정 기준
- 애매할 때는 다음 우선순위를 따른다.
  - 기능이 실제로 동작하는가
  - 기존 버그를 악화시키지 않는가
  - 중복과 복잡도를 줄였는가
  - 성능 민감 경로를 악화시키지 않는가
  - 이후 하네스 작성과 재현 자동화가 쉬워졌는가

## 금지에 가까운 안티패턴
- 같은 의미의 상태값/분기를 다른 이름으로 또 추가하는 것
- 이미 존재하는 로직을 검색하지 않고 유사 코드를 한 번 더 만드는 것
- 세이브/설치물/컨베이어 핵심 경로에 디버그용 임시 예외 처리를 남기는 것
- 대형 파일에 임시 패치를 누적하고 정리하지 않는 것

## 권장 산출물
- 가능하면 다음 산출물을 선호한다.
  - 재현 가능한 테스트 하네스
  - 명시적인 디버그 진입점
  - 책임이 분리된 helper 또는 service
  - 추후 asmdef 분리가 쉬운 폴더/namespace 구조
