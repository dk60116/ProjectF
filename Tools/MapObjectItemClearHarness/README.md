# MapObject Item Clear harness

`Run.ps1` extracts the production saved-item clearing and preservation methods. It checks that live and unloaded Desk manuals are preserved while ordinary stored items, collection storage, robot-arm cargo, production state and fuel are still cleared. Unity is not launched.
